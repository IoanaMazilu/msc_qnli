# the premise and hypothesis do not contain any numerical entities or quantities, thus no variables are defined
# the hypothesis does not contradict the premise, but also cannot be fully and explicitly entailed from it

label = "neutral"

print(label)
