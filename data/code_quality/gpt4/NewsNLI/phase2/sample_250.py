# There are no numerical entities in this example, so no variables to define

# the hypothesis refers to the exact same quote from Armstrong, as mentioned in the premise
# so, it can be fully and explicitly entailed from the premise
label = "entailment"

print(label)
