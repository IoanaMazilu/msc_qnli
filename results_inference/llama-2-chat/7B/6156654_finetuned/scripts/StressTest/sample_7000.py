# defining the variables
ramesh_days = 60
sushil_days = 25

# comparing the hypothesis with the premise
if ramesh_days < ramesh_days + sushil_days:
    # checking if the hypothesis contradicts the premise
    label = "contradiction"
elif sushil_days!= sushil_days + ramesh_days:
    # checking if the hypothesis contradicts the premise
    label = "contradiction"
else:
    # if the hypothesis does not contradict the premise
    label = "entailment"

print(label)
