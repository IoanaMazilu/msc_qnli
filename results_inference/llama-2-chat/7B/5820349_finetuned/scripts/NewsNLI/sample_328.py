# There are no numerical entities in the sentences, so no variables are defined.
# However, the hypothesis is a rephrase of the premise, so the label is entailment.

label = "entailment"

print(label)
