# No numerical information to compare, so no variables to define

# The hypothesis and premise are identical in meaning, but the range of sums is different
if hypothesis == premise:
    label = "entailment"
elif hypothesis!= premise:
    label = "contradiction"

print(label)
